import { ClickOutsiddeDirective } from './click-outsidde.directive';

describe('ClickOutsiddeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsiddeDirective();
    expect(directive).toBeTruthy();
  });
});
